[package]
name = "engine"
version = "0.1.0"
edition = "2024"

[dev-dependencies]
pretty_assertions = "1"

[dependencies]
serde = { version = "1.0.228", features = ["derive"] }
ai     = { workspace = true }
